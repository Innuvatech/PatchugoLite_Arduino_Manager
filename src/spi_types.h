#ifndef SPI_TYPES_H
#define SPI_TYPES_H

#include <Arduino.h>
#include <SPI.h>
#ifndef PATCHUGO_SPI_TYPES
#error "spi_types.h is private inside the PatchugoLite library and cannot be included in other files!"
#endif

#endif